# PostgreSQL (copy to .env and adjust as needed)
# Used when running docker compose; defaults in docker-compose.yml apply if .env is absent.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
